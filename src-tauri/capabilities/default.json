{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main launcher window.",
  "windows": ["main", "screenshot", "pin-*"],
  "permissions": ["core:default"]
}
